<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.will_s.school.sudoku"
    android:versionCode="1"
    android:versionName="1.0" android:installLocation="auto">

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:restoreAnyVersion="false"
        android:theme="@style/AppTheme" >
        <activity
            android:name="me.will_s.school.sudoku.MainMenu"
            android:label="@string/title_activity_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="me.will_s.school.sudoku.GridView"
            android:label="@string/title_activity_grid_view" >
        </activity>
        <activity
            android:name="me.will_s.school.sudoku.Preferences"
            android:label="@string/title_activity_preferences" >
        </activity>
        <activity android:name="me.will_s.school.sudoku.SavedGridsManager" android:label="@string/title_activity_saved_grids_manager"></activity>
    </application>

</manifest>